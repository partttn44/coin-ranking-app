name: coin_ranking_app
description: Coin ranking application
publish_to: none

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  equatable: ^2.1.0
  dio: ^5.11.0
  get_it: ^9.2.1
  cached_network_image: ^3.4.1
  intl: ^0.20.3
  url_launcher: ^6.3.2
  share_plus: ^13.3.0
  flutter_svg: ^2.3.0
  flutter_screenutil: ^5.9.3
  bloc_concurrency: ^0.3.0
  stream_transform: ^2.1.1
  custom_refresh_indicator: ^4.0.1

  coin_ranking_data:
    git:
      url: https://github.com/partttn44/coin-ranking-data.git
      ref: v1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
